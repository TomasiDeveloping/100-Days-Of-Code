const myName = 'Patrick';

if (myName === 'Patrick') {
    console.log('Hello!');
}

let isLoggedIn = true;

if (isLoggedIn) {
    console.log('User is logged in!');
}

// Truthy & Falsy
const entredUserName = 'Patrick';

if (entredUserName) {
    console.log('Input is valid!');
}
